from .definitions import (PATH_CONFIG, PATH_DATA, PATH_DATA_INTERIM,
                                    PATH_DATA_PROCESSED, PATH_DATA_RAW,
                                    PATH_EXPERIMENTS, PATH_NOTEBOOKS,
                                    PATH_PROJECT_ROOT, PATH_REPORTS,
                                    PATH_SAVED_MODELS, PATH_SRC, PATH_TESTS,
                          test_path_definitions)
